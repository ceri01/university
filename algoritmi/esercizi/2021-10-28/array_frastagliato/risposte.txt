1) la variabile p e' un puntatore ad un char e viene usata per puntare all'ie-esimo delemento della stringa a, quindi *p punta al i esimo carattere.

3) perche' agrv contiene in prima posizione il nome del file.

4) argv e' un vettore di stringhe, e si puo' indicare sia con *argv[] oppure con char **argv.

**argv = puntatore a puntatore di caratteri 
*argv[] = puntatore ad un array di caratteri
di base sono la stessa cosa

5) il programma dovrebbe stampare:
uno due tre ultimo quattro

6) non e' possibile sostituire le linee 8-10 con le strcpy perche' nell'ultimo strcpy passo un indirizzo al posto della stringa da copiare

7) il programma prende la parola in posizione i e la scambia con l'ultima
