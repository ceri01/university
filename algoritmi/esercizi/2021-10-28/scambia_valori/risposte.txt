1) p e' un puntatore ad intero

2) perche' essendo un puntatore contiene un indirizzo, quindi non c'e' bisogno di usare la e commerciale per specificare l'indirizzo della variabile

4) il ciclo for nelle righe 10-14 serve per popolare l'array

5) serve per stampare l'array al contratio

6) la variabile p e' un puntatore e serve per puntare alla cella di memoria dell'array e inserirci un valore.

